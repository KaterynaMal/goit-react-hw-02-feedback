import React from "react";
// import css from './Feedback.module.css';

const SectionTitle = ({
    title
}) => {
  return(
      <div>
          <h2>{title}</h2>
        </div>
    )
}
  
export { SectionTitle };